# Notebook Figures

These lightweight curated PNG figures are extracted from embedded outputs in the
original Colab notebook, `plant_disease_project.ipynb`.

The extraction script limits the gallery to a small set of notebook outputs so
the repository can show representative figures without unpacking datasets,
copying model weights, or modifying the notebook.
